{% if jobs is iterable %}
	<ul class="gpch-jobs-list">
		{% for job in jobs %}
			<li><a href="{{ function('get_post_permalink', job.ID) }}">{{ job.post_title }}</a></li>
		{% endfor %}
	</ul>
{% else %}
	{{ no_jobs_text|e('wp_kses_post')|raw  }}
{% endif %}